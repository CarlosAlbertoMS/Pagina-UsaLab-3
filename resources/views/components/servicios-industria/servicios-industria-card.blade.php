<!-- Card -->
<div class="serviciosIndustriaCard">
    <!-- Título de la card -->
    <div class="serviciosIndustriaCardTitle">{{ $title }}</div>
    <!-- Texto de la card -->
    <div class="serviciosIndustriaCardText">{{ $text }}</div>
</div>
